\documentclass[11pt,a4paper]{article}
\usepackage{amsmath,amssymb,amsfonts,amsthm}
\usepackage{graphicx}
\usepackage[margin=1in]{geometry}
\usepackage{hyperref}
\usepackage{titling}
\usepackage{mathpazo} % Palatino font
\usepackage{microtype}
\usepackage{url}
\usepackage{parskip} %removes indentation and spacing between paras
\usepackage{enumitem}

% Custom macros for notation
\newcommand{\E}{\mathbb{E}}
\newcommand{\Prob}{\mathbb{P}}
\newcommand{\Hrate}{H_\infty}
\newcommand{\Xseq}{X_1, X_2, \dots}
\newcommand{\Xn}{X^n}

\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    citecolor=blue,
    urlcolor=blue
}

\setlength{\droptitle}{-4em}

%----------------------------------------------------------------------------------------
%   TITLE SECTION
%----------------------------------------------------------------------------------------

\title{3F7 Information Theory and Coding \\ \textit{Full Technical Report}}
\author{Liu Zihe \\ zl559}
\date{University of Cambridge \\ December 6 2025}

\begin{document}

\maketitle

\section{Introduction}

Information theory provides the mathematical framework for quantifying information and establishing fundamental limits on data compression. Shannon's Source Coding Theorem establishes the entropy $H(X)$ as the lower bound for the average code length of an independent and identically distributed (i.i.d.) source. While i.i.d. models are useful first approximations, real-world data sources---such as natural language---exhibit strong temporal dependencies (memory) and non-uniform distributions.

This report extends the investigation of the 3F7 short laboratory in two directions. First, we analyse \textbf{discrete stationary sources}, relaxing the i.i.d. assumption to model sources with memory. We investigate the \textit{entropy rate} $\Hrate$ of a stochastic process and the convergence properties of block entropies. Second, we explore \textbf{adaptive coding}, where the probability model is learned online instead of 2 separate phases (building the probability model from the complete text and encoding). We derive the Laplace estimator from Bayesian principles and evaluate the performance of adaptive arithmetic coding against static methods.

\section{Overview of Short Lab}

The short laboratory focused on compressing \texttt{hamlet.txt} (approx. 207kB using fixed-length ASCII encoding) using three classical techniques designed for i.i.d. sources.

\textbf{Shannon-Fano Coding} constructs prefix-free codes by assigning lengths $l_i = \lceil \log_2(1/p_i) \rceil$. While it guarantees unique decodability (via the Kraft inequality $\sum 2^{-l_i} \le 1$), it is suboptimal because it ignores the combinatorial nature of packing the prefix-free code tree (codewords can still be placed at shorter leaf nodes, hence optimizing our encoding)

\textbf{Huffman Coding} is the optimal symbol-by-symbol prefix code. It builds a binary tree bottom-up by iteratively merging the two least probable symbols. Our experiments confirmed that its expected length $L$ satisfies $H(X) \le L < H(X) + 1$. However, its performance is constrained by the requirement that each symbol must use an integer number of bits.

\textbf{Arithmetic Coding} overcomes the integer constraint by encoding entire sequences as sub-intervals of $[0, 1)$. By mapping a sequence $x^n$ to an interval of width $P(x^n)$, it achieves a code length $L \approx -\log_2 P(x^n)$ bits. For large $n$, the per-symbol length converges to the entropy $H(X)$. This method proved superior for longer blocks but required accurate probability models.

The limitation of all three methods in the short lab was the assumption that text is a memoryless source, ignoring the significant redundancy in English grammar and spelling.

\section{Coding for Discrete Stationary Sources}

\subsection{Theoretical Background}

We model natural language text as a sequence of random variables, specifically a \textit{discrete stochastic process}, $\mathcal{X} = \{X_i\}_{i=1}^\infty$. We distinguish between two primary classes of sources:

\textbf{Memoryless Sources (i.i.d.):}
The simplest model assumes that successive symbols are i.i.d. A source is \textit{memoryless} if the joint probability of a sequence factorizes completely into the product of its components:
\begin{equation}
    \Prob(X_1, \dots, X_n) = \prod_{i=1}^n \Prob(X_i).
\end{equation}
For such a source, the information content is additive, and the entropy of a block is simply $H(X^n) = n H(X)$. The optimal code length per symbol is thus fixed at the marginal entropy $H(X)$. The fixed-length ASCII and standard Huffman coding (without blocking) implicitly assume this model.

\textbf{Stationary Sources (Sources with Memory):}
Natural languages like English are not memoryless; the letter 'h' is much more likely to follow 't' than 'q'. To capture this, we relax the independence assumption while retaining time-invariance. A process is \textit{strictly stationary} if the joint distribution of any subsequence is invariant to time shifts. That is, for any shift $\tau$ and block length $n$:
\begin{equation}
    \Prob(X_1 = x_1, \dots, X_n = x_n) = \Prob(X_{1+\tau} = x_1, \dots, X_{n+\tau} = x_n).
\end{equation}
Stationarity implies that marginal probabilities are constant $H(X_i) = H(X_1)$ and conditional probabilities depend only on the relative history, not the absolute time.

\textbf{Entropy Rate:}
For a source with memory, the marginal entropy $H(X_1)$ overestimates the true information content because it ignores the predictability provided by the context. The fundamental limit is instead given by the \textit{entropy rate} $\Hrate$, defined as the asymptotic per-symbol entropy \cite[Def 4.1]{cover}:
\begin{equation}
    \Hrate = \lim_{n \to \infty} \frac{1}{n} H(X_1, \dots, X_n).
\end{equation}
This rate represents the \textit{irreducible randomness} of the process after all past history is accounted for. To estimate $\Hrate$, we define two sequences of block entropies:
\begin{align}
    H_n   & = \frac{1}{n} H(X_1, \dots, X_n) \quad \text{(Entropy per symbol of $n$-block)} \\
    H_n^* & = H(X_n | X_{n-1}, \dots, X_1) \quad \text{(Conditional entropy)}
\end{align}

\subsection{Convergence of Entropy Rates (Theorem 1)}

For stationary sources, these sequences converge to the entropy rate $\Hrate$ as $n \to \infty$. We provide proofs for the properties stated in the FTR handout, referencing Cover \& Thomas \cite[Chap 4]{cover}.

\textbf{Statement 1: $H_{n+1}^* \le H_n^*$} (Conditional entropy is non-increasing). \\
\textit{Proof:}
\begin{align*}
    H_{n+1}^* & = H(X_{n+1} | X_n, \dots, X_2, X_1)                                          \\
              & \le H(X_{n+1} | X_n, \dots, X_2) \quad \text{(Conditioning reduces entropy)} \\
              & = H(X_n | X_{n-1}, \dots, X_1) \quad \text{(By Stationarity)}                \\
              & = H_n^* \quad \square
\end{align*}

\textbf{Statement 2: $H_{n+1} \le H_n$} (Per-symbol entropy is non-increasing). \\
\textit{Proof:} By the chain rule, $H(X_1, \dots, X_{n+1}) = H(X_1, \dots, X_n) + H(X_{n+1} | X_1, \dots, X_n)$. Then
\begin{equation}
    H_{n+1} = \frac{H(X_1, \dots, X_{n+1})}{n+1} = \frac{n H_n + H_{n+1}^*}{n+1}
\end{equation}
Since $H_{n+1}^* \le H_n^*$ (from Statement 1), $H_n^* \le H_n$ (from statement 3), then $H_{n+1}^* \le H_n^* \le H_n$ where $H_n$ is the average of joint entropy across the $n$ terms. Adding a term $H_{n+1}^*$ that is smaller than or equal to the previous average must reduce (or maintain) the new average. Thus $H_{n+1} \le H_n$. $\square$

\textbf{Statement 3: $H_n^* \le H_n$.} \\
\textit{Proof:} From the Chain Rule for entropy,
\begin{equation}
    H(X_1, \dots, X_n) = \sum_{i=1}^n H(X_i | X_{i-1}, \dots, X_1).
\end{equation}
Dividing by $n$:
\begin{equation}
    H_n = \frac{1}{n} \sum_{i=1}^n H_i^*.
\end{equation}
Since $H_i^*$ is a non-increasing sequence (Statement 1), the current term $H_n^*$ is less than or equal to all preceding terms $H_i^*$ for $i < n$. Therefore, the average $H_n$ must be greater than or equal to the minimum term $H_n^*$. $\square$

\textbf{Statement 4: $\lim_{n \to \infty} H_n = \lim_{n \to \infty} H_n^* = \Hrate$.} \\
\textit{Proof:} Since $H_n^*$ is non-increasing (Statement 1) and bounded below by 0 (entropy is non-negative), the limit $\lim_{n \to \infty} H_n^* = H'$ exists. Since $H_n$ is the arithmetic mean (Cesàro mean) of the sequence $H_n^*$ as $n \to \infty$, by the \textit{Cesàro Mean Theorem} (if $a_n \to a$, then $\frac{1}{n}\sum a_i \to a$), the sequence of averages converges to the same limit \cite[Thm 4.2.1]{cover}. This limit is defined as the \textit{entropy rate} $\Hrate$. $\square$


\subsection{Experimental Results: Entropy Rate Convergence}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.75\textwidth]{images/entropy_rate_plot.png}
    \caption{Entropy rate measurements on \texttt{hamlet.txt}. The estimates converge to 0 for large $n$ due to data sparsity, not because the text is deterministic.}
    \label{fig:entropy_rate}
\end{figure}

We measured the block entropy per symbol, $H_n = \frac{1}{n} H(X_1, \dots, X_n)$, for $n=1$ to $100$. As shown in Fig. \ref{fig:entropy_rate}, estimates for small $n$ ($n=1 - 20$) decrease quickly, correctly reflecting that English text is a source with memory; knowledge of previous characters reduces the uncertainty of the current one. However, for $n \ge 4$, the estimate continues to decay toward zero. The true entropy rate of English ($H_\infty$) is surely not zero. Rather, this decay is a statistical artifact of an maximum likelihood estimator (of the entropy rate) operating in a high-dimensional regime with insufficient data (the "curse of dimensionality"). Accurate measurement of $H_\infty$ is impossible without a sample size $N$ that scales as $|\mathcal{X}|^n$.

\subsection{Implications for Static Coding}

The convergence failure observed in highlights a critical limitation for static source coding. To approach the entropy rate of a stationary source, the encoder must exploit a context of length $n$. In a static framework, this requires transmitting the probability model (e.g., a frequency table) to the decoder as side information. The size of this model is proportional to $|\mathcal{X}|^n$. As we increase $n$ to capture longer dependencies, this overhead grows exponentially, quickly exceeding the size of the compressed data itself. Consequently, static methods cannot simply let $n \to \infty$; they are fundamentally limited to a small, finite $n$.

\subsection{Context-Adaptive Arithmetic Coding}

To overcome the static overhead limit, we employ \textit{Context-Adaptive Arithmetic Coding}, often formalized as \textit{Prediction by Partial Matching (PPM)}. Instead of transmitting a massive static table, the model consists of a set of conditional probability distributions $P(X_t | C)$ that are built adaptively during encoding, where $C$ is the context $(X_{t-n}, \dots, X_{t-1})$.

This approach directly addresses the data sparsity problem. When a symbol appears in a context $C$ that has zero frequency (has never been seen before), encoder and decoder then "back off" to a shorter context of length $n-1$, recursing down to order-0 if necessary. By using this escape mechanism, the algorithm can exploit very large $n$ for frequent patterns (where statistics are rich) while falling back to shorter contexts for rare sequences. This allows the compression rate to approach $\Hrate$ without the prohibitive $|\mathcal{X}|^n$ overhead required by static methods.

\section{Adaptive Coding}

\subsection{Bayesian Derivation of the Laplace Estimator}

In adaptive coding, we face the "zero-frequency problem": if a symbol $x$ has not yet appeared in the stream, its Maximum Likelihood Estimate (MLE) is $P(x) = 0$. This is catastrophic for arithmetic coding. To solve this, we must assign probability mass to unseen events. We derive the solution using Bayesian inference, which treats the unknown probabilities not as fixed values, but as random variables describing our uncertainty \cite[p. 97]{godsill}.

\subsubsection{The Beta Posterior}
Consider a binary version of the problem where we simply estimate the probability $\theta$ that the next symbol is a specific character 'x'. We start with \textbf{Bayes' Theorem}:
\begin{equation}
    P(\theta | D) = \frac{P(D | \theta) P(\theta)}{P(D)}
\end{equation}
where $D$ is the observed data (sequence of symbols), $P(\theta)$ is the \textit{prior} belief, and $P(D|\theta)$ is the \textit{likelihood} of the data.

\begin{itemize}[nosep]
    \item \textbf{Prior:}
          Since we assume no prior knowledge about the text, we must choose a prior that represents total ignorance. For a probability parameter $\theta \in [0,1]$, the natural choice is the Uniform Distribution, also known as the $Beta(1,1)$ distribution \cite[Ch 3]{mackay}. In the Bayesian framework, this corresponds to a prior which expresses no initial preference for one parameter set over another:
          \begin{equation}
              P(\theta) = 1 \quad \text{for } 0 \le \theta \le 1
          \end{equation}
    \item \textbf{Likelihood:}
          Suppose we observe a stream of length $n$ containing $n_x$ occurrences of 'x' and $n_y$ occurrences of other symbols ($n_y = n - n_x$). Assuming independence given $\theta$, the probability of seeing this specific sequence is:
          \begin{equation}
              P(D | \theta) = \theta^{n_x} (1-\theta)^{n_y}.
          \end{equation}

    \item \textbf{Posterior:}
          Multiplying the prior and likelihood gives us the posterior distribution (proportionality ignores the normalizing constant $P(D)$):
          \begin{equation}
              P(\theta | D) \propto \theta^{n_x} (1-\theta)^{n_y} \cdot 1.
          \end{equation}
\end{itemize}

This resulting distribution is a Beta distribution with parameters $(n_x+1, n_y+1)$. (The general form of the Beta probability density function is: $f(\theta; \alpha, \beta) \propto \theta^{\alpha-1}(1-\theta)^{\beta-1}$) As $n$ grows, this curve becomes a sharp peak around the empirical frequency $n_x/n$, representing our growing confidence.

\subsubsection{Laplace's Rule of Succession (Binary Case)}
To encode the \textit{next} symbol $X_{n+1}$, we do not simply use the most probable value of $\theta$ (the peak). Instead, we must average over \textit{all possible} values of $\theta$, weighted by their posterior probability - a process known as \textit{marginalization}. This predictive probability is equivalent to the expected value of the posterior distribution:
\begin{align}
    P(X_{n+1} = x | D) & = \int_0^1 P(X_{n+1} = x | \theta) P(\theta | D) \, d\theta                                                          \\
                       & = \int_0^1 \theta \cdot P(\theta | D) \, d\theta \quad \text{since $P(X_{n+1} = x | \theta) = \theta$ by definition} \\
                       & = \mathbb{E}[\theta | D]
\end{align}
This expectation corresponds to the Minimum Mean-Squared Error (MMSE) estimator in Bayesian theory. Since our posterior is a Beta distribution $Beta(\alpha, \beta)$ with $\alpha = n_x+1$ and $\beta = n_y+1$, we use the known formula for the mean of a Beta distribution, $\mu = \frac{\alpha}{\alpha+\beta}$ \cite{mackay,weisberg}:
\begin{equation}
    \hat{p}(x) = \mathbb{E}[\theta | D] = \frac{\alpha}{\alpha + \beta} = \frac{n_x + 1}{(n_x + 1) + (n_y + 1)} = \frac{n_x + 1}{n + 2}.
\end{equation}
This result is Laplace's Rule of Succession. It effectively adds 1 "pseudocount" to both outcomes, preventing zero probabilities \cite[p. 52]{mackay}.

\subsubsection{Generalization to Text (The Dirichlet Distribution)}

We now generalize from binary outcomes to an alphabet of size $K$ (e.g., $K=256$ for extended ASCII). The unknown parameter is now a probability vector $\boldsymbol{\theta} = (\theta_1, \dots, \theta_K)$ where $\sum_{i=1}^K \theta_i = 1$.

The natural generalization of the Beta distribution to $K$ dimensions is the \textit{Dirichlet distribution} \cite[pg 317]{mackay}. Just as the Beta distribution represents our uncertainty about a single probability parameter, the Dirichlet distribution represents our uncertainty about a \textit{vector} of probabilities that must sum to 1. The Dirichlet distribution with parameters $\boldsymbol{\alpha} = (\alpha_1, \dots, \alpha_K)$ has probability density \cite[Eq. 23.30]{mackay}:
\begin{equation}
    P(\boldsymbol{\theta} | \boldsymbol{\alpha}) = \frac{1}{B(\boldsymbol{\alpha})} \prod_{i=1}^K \theta_i^{\alpha_i - 1}
\end{equation}
where $B(\boldsymbol{\alpha}) = \frac{\prod_i \Gamma(\alpha_i)}{\Gamma(\sum_i \alpha_i)}$ is the multivariate Beta function (normalizing constant) and each $\alpha_i > 0$ \cite{tsun2025}.

Suppose we observe a sequence with symbol counts $\mathbf{n} = (n_1, \dots, n_K)$ where $N = \sum_i n_i$ is the total number of observations.

\begin{itemize}[nosep]
    \item \textbf{Prior:} We start with a uniform prior representing complete ignorance, which corresponds to $\boldsymbol{\alpha} = (1, \dots, 1)$:
          \begin{equation}
              P(\boldsymbol{\theta}) = \text{Dir}(1, \dots, 1) = \text{Uniform}
          \end{equation}

    \item \textbf{Likelihood:} The probability of observing these counts follows the multinomial distribution:
          \begin{equation}
              P(\mathbf{n} | \boldsymbol{\theta}) \propto \prod_{i=1}^K \theta_i^{n_i}
          \end{equation}

    \item \textbf{Posterior:} By Bayes' theorem, the posterior is also Dirichlet with updated parameters:
          \begin{equation}
              P(\boldsymbol{\theta} | \mathbf{n}) = \text{Dir}(\alpha_1 + n_1, \dots, \alpha_K + n_K) = \text{Dir}(1 + n_1, \dots, 1 + n_K)
          \end{equation}
\end{itemize}

To predict the next symbol, we must marginalize over all possible parameter values weighted by the posterior. This predictive probability equals the \textit{mean} of the posterior Dirichlet distribution. For a Dirichlet distribution with parameters $(\alpha_1', \dots, \alpha_K')$, the mean is \cite[Eq. 23.32]{mackay}:
\begin{equation}
    \mathbb{E}[\theta_i] = \frac{\alpha_i'}{\sum_{j=1}^K \alpha_j'}
\end{equation}

Substituting our posterior parameters $\alpha_i' = 1+ n_i$:
\begin{equation}
    \hat{p}(i) = \mathbb{E}[\theta_i | \mathbf{n}] = \frac{n_i + 1}{\sum_{j=1}^K (n_j + 1)} = \frac{n_i + 1}{N + K}
\end{equation}

This is the \textit{Laplace estimator}. It ensures every symbol has non-zero probability of at least $1/(N+K)$, solving the zero-frequency problem. The uniform prior $\boldsymbol{\alpha} = (1, \dots, 1)$ effectively adds 1 "pseudocount" to each symbol, which can be interpreted as pretending we observed each symbol once before seeing any real data \cite{tsun2025,mackay}.

\subsection{Experimental Performance and Analysis}

We simulated adaptive arithmetic coding on \texttt{hamlet.txt}. The code length $L$ for a sequence $x_1, \dots, x_n$ is estimated as the sum of the log-probabilities assigned by the model at each step:
\begin{equation}
    L = - \sum_{i=1}^n \log_2 \hat{p}_{x_i}
\end{equation}

Note that the probability model here built is a still a 0-order adaptive model (we are still learning the distribution assuming each character is i.i.d.) The model is adaptive in the sense of updating its probabilities as more characters are observed.

\subsubsection{Convergence to Static Entropy}
Figure \ref{fig:convergence} shows the running average rate $L/n$, which is the estimate of the entropy of the i.i.d distribution.

\begin{itemize}
    \item \textbf{Initial Behaviour:} For small $n$, the rate is high ($>6$ bits) because counts are low and the uniform prior dominates the distribution.
    \item \textbf{Asymptotic Behavior:} As $n \to \infty$, the estimator $\hat{p}_x$ converges to the true relative frequencies of the file. Consequently, the adaptive rate converges to the static i.i.d. entropy $H_1$ ($\approx 4.45$ bits). Note that the curve stays slightly above $H_1$. This redundancy represents the "learning cost" of building the model online (we haven't observed the complete data yet)
\end{itemize}


\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{images/adaptive_coding_convergence.png}
    \caption{Convergence of 0-order Adaptive Coding ($L/n$) to the static entropy $H_1$ with $\delta=1.0$ (Laplace Estimator) over $N=10,000$ symbols.}
    \label{fig:convergence}
\end{figure}


\subsubsection{Sensitivity to Bias Parameter $\delta$}

We investigated the impact of the pseudocount parameter $\delta$ by generalizing the estimator to:
\begin{equation}
    \hat{p}(x) = \frac{n_x + \delta}{N + K\delta}
\end{equation}
We tested $\delta \in [0.01, 50]$ on the first $N=10,000$ symbols of \texttt{hamlet.txt} to visualize convergence behavior (Fig. \ref{fig:delta_grid}), and then measured the final compression rate on the \textit{entire text} ($N=207,039$) to determine the optimal parameter (Fig. \ref{fig:delta_final}).

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.9\textwidth]{images/delta_convergence_grid.png}
    \caption{Adaptive coding convergence profiles for various prior strengths $\delta$ ($N=10,000$ symbols). Small $\delta$ leads to rapid learning but high volatility; large $\delta$ creates a stable but slow-converging model.}
    \label{fig:delta_grid}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{images/delta_variation.png}
    \caption{Effect of bias $\delta$ on the final compression rate, measured on the \textit{entire} Hamlet text ($N \approx 207k$)}
    \label{fig:delta_final}
\end{figure}

\begin{itemize}
    \item \textbf{Small $\delta$ ($\delta < 0.5$):}
          When $\delta$ is very small (e.g., 0.01), the model assigns negligible probability mass to unseen characters. This makes the "zero-frequency" penalty extremely harsh. During the first appearance of a symbol ($n_x=0$), the estimated probability is $P \approx \delta/N$, leading to a code length of $-\log_2(\delta/N)$. As $\delta \to 0$, this initial cost spikes dramatically, as seen in the sharp initial peaks in Fig. \ref{fig:delta_grid}. However, once a symbol is seen, the model adapts very quickly.

    \item \textbf{Large $\delta$ ($\delta > 1.0$):}
          A large $\delta$ (e.g., 50.0) corresponds to a strong prior belief that the distribution is uniform. It dominates the denominator $N + K\delta$, diluting the influence of the observed data counts $n_x$. This acts as a "dampener," resulting in a very stable curve that converges slowly to the true entropy (Fig. \ref{fig:delta_grid}, bottom right).

    \item \textbf{Optimal $\delta = 0.5$:}
          Our experiments on the full text (Fig. \ref{fig:delta_final}) show that $\delta=0.5$ yields the minimum code rate, slightly outperforming the Laplace estimator ($\delta=1$)
\end{itemize}

\subsection{Comparison of Adaptive vs. Static Coding}

To rigorously compare the performance of Adaptive and Static coding, we invoke the \textit{Minimum Description Length (MDL)} principle. \footnote{Interestingly enough, I actually wrote a paper using the Bayesian Information Criterion, so I'm delighted to see its use here. See \url{https://arxiv.org/abs/2409.04913}} This principle states that the total description length $L(x_1 \dots x_n)=L(x^n)$ is composed of the cost to encode the data given a model, plus the cost of specifying the probability model itself:
\begin{equation}
    L(x^n) = L(\text{Data} \mid \text{Model}) + L(\text{Model})
\end{equation}

In our experiment, we compare the cumulative code lengths of the two approaches as a function of the sequence length $n$:

\textbf{Static Coding (Theoretical Bound):}
Since static coding requires a pre-computed frequency table, it is impossible to run "online" in the same manner as adaptive coding. Instead, we plot the theoretical lower bound at each length $n$. This bound assumes that for any prefix of length $n$, we transmit the optimal frequency table followed by the data. The cost is calculated using the standard MDL penalty \cite[Ch. 28]{mackay}:
\begin{equation}
    L_{\text{static}}(n) \approx \underbrace{n H_n}_{\text{Data Cost}} + \underbrace{\frac{K-1}{2} \log_2 n}_{\text{Model Cost}}
\end{equation}
The term $\frac{K-1}{2} \log_2 n$ represents the parametric complexity \footnote{The "Occam Factor", the cost of specifying $K-1$ independent probability parameters to a precision of $1/\sqrt{n}$}

\textbf{Adaptive Coding (Actual Cost):}
This curve represents the number of bits generated by the simulated arithmetic coder up to symbol $n$. Unlike the static case, there is no explicit model header ($L(\text{Model}) = 0$). Instead, the cost is the sum of the negative log-probabilities assigned by the sequential estimator:
\begin{equation}
    L_{\text{adaptive}}(n) = \sum_{i=1}^n -\log_2 \hat{P}(x_i \mid x^{i-1})
\end{equation}
The "redundancy" in this case is the difference between this sum and the ideal entropy $n H_n$. This difference represents the "learning penalty" incurred while the estimator $\hat{P}$ converges from the uniform prior to the true statistics.

\subsubsection{Experimental Results}
Figure \ref{fig:adaptive_vs_static} presents the comparison of these two quantities on the \texttt{hamlet.txt} sequence.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{images/adaptive_vs_nonadaptive.png}
    \caption{Comparison of Adaptive Coding (actual cumulative bits) versus Static Coding (theoretical data + MDL model cost). The curves run parallel for large $n$, confirming that the adaptive learning penalty scales identically to the static model overhead.}
    \label{fig:adaptive_vs_static}
\end{figure}

\begin{itemize}
    \item \textbf{Short Sequences ($n < 10^4$):} Adaptive coding is superior. The static method suffers from the "overhead problem," where the fixed cost of the frequency table ($\approx \frac{K}{2} \log n$) outweighs the data payload. Adaptive coding avoids this upfront cost, making it ideal for short messages.
    \item \textbf{Convergence ($n \to \infty$):} As $n$ increases, the two curves converge. The static overhead is amortized over more symbols, while the adaptive estimator converges to the true frequencies. The crossover point marks the length where the static header cost becomes negligible compared to the accumulated learning redundancy.
\end{itemize}

Ultimately, this demonstrates that adaptive coding achieves the same asymptotic efficiency as static coding without requiring a two-pass process.

\subsection{Adaptive Coding with Memory}
The experiment above used a 0-order model (assuming i.i.d.). To capture memory (where $\Hrate < H_1$), we would need to estimate conditional probabilities $P(X_t | X_{t-1})$.
However, extending Eq. \ref{eq:estimator} to high-order contexts introduces \textbf{Context Dilution}. As context length increases, the total count $n$ is split among $|\mathcal{X}|^k$ distinct contexts. Most contexts appear only a few times, so $n_{context} \approx 0$.
In this regime, the bias $\delta$ dominates the counts ($n_x \ll \delta$), resulting in a near-uniform distribution and poor compression. Practical schemes like PPM solve this by "escaping" to shorter contexts when statistics are sparse.

\section{Conclusion}

This report demonstrated that while English text is a stationary source with memory, exploiting this theoretically (via $\Hrate$) is hindered by sparse data. Adaptive coding offers a robust solution, allowing compression to approach the source entropy without prior knowledge of statistics. By deriving the Laplace estimator from Bayesian principles, we showed that adding a "pseudocount" of 1 is not heuristic but the optimal strategy under a uniform prior assumption. This principle underpins modern compressors like LZ77 and prediction models in Large Language Models.

%----------------------------------------------------------------------------------------
%   BIBLIOGRAPHY
%----------------------------------------------------------------------------------------

\begin{thebibliography}{9}

    \bibitem{cover}
    T. M. Cover and J. A. Thomas, \textit{Elements of Information Theory}, 2nd ed. Wiley-Interscience, 2006.

    \bibitem{mackay}
    D. J. C. MacKay, \textit{Information Theory, Inference, and Learning Algorithms}. Cambridge University Press, 2003.

    \bibitem{godsill}
    S. Godsill, \textit{Estimation Theory and Inference Methods}, Lecture Notes (3F3), University of Cambridge, 2023

    \bibitem{weisberg}
    J. Weisberg, "Laplace's Rule of Succession," \textit{Formal Epistemology}, 2019. Available: \url{https://jonathanweisberg.org/post/inductive-logic-2/}

    \bibitem{tsun2025}
    A. Tsun, ``The Beta and Dirichlet Distributions,'' Chapter 7.4 in \textit{Probability \& Statistics with Applications to Computing}, CS109 Course Reader, Stanford University, 2025. Available: \url{https://web.stanford.edu/class/archive/cs/cs109/cs109.1218/files/student_drive/7.4.pdf}

    \bibitem{xing2014}
    E. P. Xing, ``Bayesian Nonparametrics: Dirichlet Processes,'' Lecture 19 in \textit{10-708: Probabilistic Graphical Models}, Lecture notes by C. Malings and J. Gao, Carnegie Mellon University, Spring 2014. Available: \url{https://www.cs.cmu.edu/~epxing/Class/10708-14/scribe_notes/scribe_note_lecture19.pdf}


\end{thebibliography}

\end{document}